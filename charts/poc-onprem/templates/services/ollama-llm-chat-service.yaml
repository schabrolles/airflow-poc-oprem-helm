apiVersion: v1
kind: Service
metadata:
  name: ollama-llm-chat
  namespace: {{ include "poc-onprem.namespace" . }}
  labels:
    app: ollama-llm-chat
spec:
  type: ClusterIP
  ports:
  - port: 11434
    targetPort: 11434
    protocol: TCP
    name: ollama
  selector:
    app: ollama-llm-chat

# Made with Bob
