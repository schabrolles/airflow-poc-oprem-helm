apiVersion: v1
kind: Service
metadata:
  name: embedding-service
  namespace: {{ include "poc-onprem.namespace" . }}
  labels:
    app: embedding-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: embedding-service

# Made with Bob
