apiVersion: v1
kind: Service
metadata:
  name: pg-airflow-db
  namespace: {{ include "poc-onprem.namespace" . }}
  labels:
    app: pg-airflow-db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: pg-airflow-db

# Made with Bob
