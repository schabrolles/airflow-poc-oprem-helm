apiVersion: v1
kind: Secret
metadata:
  name: postgres-airflow-secrets
  namespace: {{ include "poc-onprem.namespace" . }}
  labels: {{- include "poc-onprem.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgres-user: {{ .Values.postgresql.airflow.credentials.username | quote }}
  postgres-password:
    {{ .Values.postgresql.airflow.credentials.password | quote }}
  postgres-db: {{ .Values.postgresql.airflow.credentials.database | quote }}

# Made with Bob
